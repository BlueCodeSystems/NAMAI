---
name: step4_ipv_suspect
description: IPV Suspected
priority: 1
condition: "(!step4_ipv_signs_symptoms.contains('none') && !step4_ipv_signs_symptoms.isEmpty()) || (!global_ipv_physical_signs_symptom.contains('none') && !global_ipv_signs_symptoms.isEmpty())"
actions:
  - "calculation = 1"